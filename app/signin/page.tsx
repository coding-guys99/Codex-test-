'use client';
import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {getSupabase} from '@/lib/supabase';
export default function SignIn(){const [msg,setMsg]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const sb=getSupabase();if(!sb){setMsg('Supabase environment is not connected yet.');return}const {error}=await sb.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(error)setMsg(error.message);else location.href='/dashboard'}return <><header className="topbar"><div className="container nav"><Link className="brand" href="/">BORNEO / BUSINESS</Link></div></header><main><form className="auth" onSubmit={submit}><div className="eyebrow">Member access</div><h2>Sign in</h2><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="btn primary" style={{width:'100%'}}>Sign In</button><p className="meta">{msg}</p><p className="meta">New here? <Link href="/onboarding">Create a company profile</Link></p></form></main></>}
