import Link from 'next/link'; import data from '@/data/opportunities';
export default function Market(){const counts=['Sarawak','Sabah','Brunei'].map(r=>[r,data.filter((x:any)=>x.region===r).length] as const);return <><header className="topbar"><div className="container nav"><Link className="brand" href="/">BORNEO / BUSINESS</Link></div></header><main className="section"><div className="container"><div className="eyebrow">Market intelligence</div><h2>Borneo market coverage</h2><p className="sub">Coverage counts are calculated from the verified V1 official-source dataset.</p><div className="kpis">{counts.map(([r,c])=><div className="kpi" key={r}><strong>{c}</strong><span>{r} opportunities</span></div>)}<div className="kpi"><strong>{new Set(data.map((x:any)=>x.industry)).size}</strong><span>Industries classified</span></div></div></div></main></>}
